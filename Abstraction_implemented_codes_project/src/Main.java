/* instantiate Stack, LinearQueue, CircularQueue, or LinkedList here
    Instantiating Stack -- Stack <identifier> = new Stack(size);
    for example : Stack List1 = new Stack(10);
    Instantiating LinearQueue -- LinearQueue <identifier> = new LinearQueue(size);
    Instantiating CircularQueue -- CircularQueue <identifier> = new CircularQueue(size);
    Instantiating LinkedList -- LinkedList <identifier> = new LinkedList(size);
    Access methods, with respect to Programmer's Manual
    Accessing methods -- For procedure -- <ObjectIdentifier>.MethodName(parameter);
    Accessing methods -- For functions -- <identifier> = <ObjectIdentifier>.MethodName(parameter);
    for example: if adding value 5 in stack, List1.push(5);
                                                                    - Aditya Karki
*/

// Creating array of Abstract data types
public class Main {
    public static void main(String[] args) {

    }
}
